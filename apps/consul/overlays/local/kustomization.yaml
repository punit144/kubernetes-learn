apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: consul-local

namespace: consul

bases:
  - ../../base

namePrefix: consul-local-

patchesStrategicMerge:
  - consul-patch.yaml
