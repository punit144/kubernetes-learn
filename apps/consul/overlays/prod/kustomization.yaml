apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: consul-prod

namespace: consul-prod

resources:
  - namespace.yaml

bases:
  - ../../base

namePrefix: consul-prod-

# Production-specific replicas (highest availability)
replicas:
  - name: consul
    count: 5
